// About Section

const AboutSection = ({ aboutRef, inView }) => {
  return (
    <section
      id="about"
      ref={aboutRef}
      className="home-page__section home-page__section-2"
    ></section>
  );
};

export default AboutSection;
